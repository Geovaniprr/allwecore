# Copie para .env e preencha os valores reais.
FLASK_ENV=development
SECRET_KEY=troque-por-uma-chave-secreta-forte

# Contato / captação de leads
WHATSAPP_NUMBER=5511971446403
CONTACT_EMAIL=allwecore@gmail.com

# Rastreamento (deixe vazio em dev)
GA4_ID=
META_PIXEL_ID=
GTM_ID=

# Armazenamento de leads (MVP: arquivo CSV local — mantido como backup)
LEADS_FILE=leads.csv

# Destino dos leads
LEAD_TO=allwecore@gmail.com

# --- Produção (Vercel): envio via Resend (API HTTP) — tem prioridade ---
RESEND_API_KEY=          # pegue em resend.com/api-keys — NUNCA preencher aqui, só no .env / painel da Vercel
LEAD_FROM=All We Core <onboarding@resend.dev>

# --- Desenvolvimento local: fallback via Gmail SMTP ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=allwecore@gmail.com
SMTP_PASS=                # senha de app do Gmail (16 dígitos) — NUNCA preencher aqui, só no .env
